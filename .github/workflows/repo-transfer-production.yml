# name: Repository Transfer Production

# on:
#   push:
#     branches: [main]
#     paths:
#       - 'transfer_repos.csv'

# jobs:
#   transfer:
#     name: Repository Transfer Execution
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3
#         with:
#           ref: 'main'

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.12'

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

#       - name: Final dry-run verification
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN_ADMIN }}
#         run: |
#           python repo_transfer.py --csv transfer_repos.csv --dry-run --auto-approve
#           echo "Dry run completed. Check the logs to verify the transfers."
#           echo "To proceed with actual transfers, manually approve this workflow."

#       - name: Wait for manual approval
#         uses: trstringer/manual-approval@v1
#         with:
#           secret: ${{ secrets.GITHUB_TOKEN }}
#           approvers: ${{ vars.TRANSFER_APPROVERS }}
#           minimum-approvals: 1
#           issue-title: "Approve repository transfers"
#           issue-body: "Please approve the repository transfers by adding a comment with 'approved' in this issue."
#           exclude-workflow-initiator-as-approver: false

#       - name: Execute actual transfers
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN_ADMIN }}
#         run: |
#           python repo_transfer.py --csv transfer_repos.csv --auto-approve

#       - name: Upload logs
#         uses: actions/upload-artifact@v3
#         with:
#           name: repo-transfer-production-logs
#           path: logs/repo_transfer_*.log
#           retention-days: 14
